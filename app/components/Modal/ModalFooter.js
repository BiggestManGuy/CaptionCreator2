import styled from 'styled-components';

import colors from 'styles/colors';

export default styled.div`
  margin-top: 1rem;
  padding-top: 0.5rem;
  border-top: 1px solid ${colors.onPrimaryDark};
`;
