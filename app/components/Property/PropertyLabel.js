import styled from 'styled-components';

export default styled.h2`
  display: inline-block;
  height: 0.75rem;
  margin: 0 0 0 6px;
  padding: 0;
  font-size: 0.75rem;
  font-weight: normal;
  white-space: nowrap;
`;
