// import { selectCaptionLayersDomain } from '../selectors';

describe('selectCaptionLayersDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
