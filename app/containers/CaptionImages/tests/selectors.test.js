// import { selectCaptionImagesDomain } from '../selectors';

describe('selectCaptionImagesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
