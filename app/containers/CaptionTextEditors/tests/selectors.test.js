// import { selectCaptionTextEditorDomain } from '../selectors';

describe('selectCaptionTextEditorDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
